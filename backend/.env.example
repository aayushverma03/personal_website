# Copy into ../.env (repo root) or ./.env (backend) and fill in.
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5.4-mini

# Session signing + cookie
SESSION_SECRET=change-me-in-production
SESSION_COOKIE_SECURE=false
