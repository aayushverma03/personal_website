export const metadata = {
  title: "LexDraft",
  description:
    "Draft sharp NDAs, offer letters, DPAs, and more by chatting with an AI.",
};

export default function PrelegalLayout({ children }) {
  return <section className="prelegal-route">{children}</section>;
}
